# 寻野酒吧小程序（顾客端）设计文档 v2.0

## 一、项目概述

寻野酒吧小程序是一个面向顾客的微信小程序，提供扫码点餐、在线支付、订单跟踪、会员服务、消息通知等功能。

### 设计理念

基于HTML原型的设计风格，打造高端、简约、沉浸式的点餐体验：
- **暗黑系设计**：深色背景营造酒吧氛围
- **金色点缀**：使用金色作为主题色，体现高端品质
- **卡片式布局**：清晰的信息层级
- **流畅动画**：页面切换和交互动画提升体验

### 技术栈
- **前端框架**: 微信小程序原生框架
- **后端接口**: Spring Boot REST API
- **数据存储**: 本地缓存（wx.storage）+ 后端MySQL + Redis

---

## 二、设计规范

### 2.1 色彩方案
```css
--bg-app: #050507;           /* 主背景 */
--bg-card: #1A1A1A;          /* 卡片背景 */
--color-gold: #D6A85A;       /* 金色主题 */
--text-main: #FFFFFF;        /* 主文字 */
--text-gray: #6F6A63;        /* 次要文字 */
--border-color: #2A2A2A;     /* 边框颜色 */
```

### 2.2 字体规范
- **标题**: bold, 32-40rpx
- **正文**: normal, 28rpx
- **辅助文字**: 24rpx
- **小字**: 20rpx

### 2.3 圆角规范
- **大卡片**: 32rpx
- **中卡片**: 24rpx
- **小卡片/按钮**: 16rpx
- **圆形按钮**: 50%

### 2.4 间距规范
- **页面边距**: 32rpx
- **卡片间距**: 24rpx
- **内容间距**: 16rpx

---

## 三、功能模块

### 3.1 核心功能
1. **扫码点餐** - 扫描桌台二维码快速进入点餐
2. **菜单浏览** - 分类展示商品，支持搜索
3. **购物车** - 商品加购、数量调整、备注
4. **在线支付** - 微信支付（当前MOCK）
5. **订单跟踪** - 实时查看订单状态
6. **会员中心** - 会员信息、积分、优惠券
7. **消息通知** - 订单通知、活动推送
8. **订单历史** - 查看历史订单

### 3.2 页面结构

#### TabBar 页面（4个）
1. **首页** (index) - 品牌展示、桌台选择、推荐商品
2. **点餐** (menu) - 菜单浏览、商品加购
3. **消息** (message) - 消息列表
4. **我的** (mine) - 会员中心、订单历史、设置

#### 子页面（7个）
5. **选桌** (table-select) - 扫码/手动选择桌台
6. **购物车** (cart) - 购物车详情、提交订单
7. **支付** (payment) - 确认支付、选择支付方式
8. **订单结果** (order-result) - 支付成功页面
9. **订单详情** (order-detail) - 订单详细信息
10. **订单历史** (order-history) - 历史订单列表
11. **登录** (login) - 手机号登录

---

## 四、页面详细设计

### 4.1 首页 (index)

#### 功能描述
- 品牌展示区（顶部渐变背景）
- 营业时间显示
- 公告栏（跑马灯）
- 当前桌台状态卡片
- 快捷操作（开始点餐、扫码点单）
- 店长推荐商品横向滚动

#### 接口依赖
- `GET /customer/products/recommendations` - 获取推荐商品
- `GET /customer/bar-tables/current` - 获取当前桌台信息

#### 交互逻辑
1. 进入页面检查是否已选桌
2. 点击"开始点餐"：已选桌→跳转菜单页，未选桌→跳转选桌页
3. 点击"扫码点单"：调用wx.scanCode扫描二维码
4. 推荐商品可直接加入购物车

---

### 4.2 选桌页 (table-select)

#### 功能描述
- 扫码选桌区域（大按钮）
- 手动输入桌号
- 可用区域展示（网格布局）
- 显示最近使用的桌台

#### 接口依赖
- `GET /customer/bar-tables` - 获取可用桌台列表
- `POST /customer/bar-tables/select` - 选择桌台

#### 交互逻辑
1. 扫码：调用wx.scanCode，解析二维码获取桌台编号
2. 手动输入：输入桌号后点击确认
3. 点击桌台卡片：直接选择该桌台
4. 选择成功后跳转到菜单页

---

### 4.3 菜单页 (menu)

#### 功能描述
- 顶部品牌名称居中
- 当前桌台标签 + 搜索按钮
- 左侧分类导航（固定宽度）
- 右侧商品列表（滚动）
- 商品卡片：图片、名称、描述、价格、加购按钮
- 底部悬浮购物车条（显示总价、数量）

#### 接口依赖
- `GET /customer/products/categories` - 获取商品分类
- `GET /customer/products?categoryId={id}` - 获取分类下的商品

#### 交互逻辑
1. 点击左侧分类：右侧商品列表切换
2. 点击加号：商品数量+1，显示减号和数量
3. 点击减号：商品数量-1，数量为0时隐藏减号和数量
4. 购物车条实时更新总价和数量
5. 点击"去结算"：跳转购物车页

---

### 4.4 购物车页 (cart)

#### 功能描述
- 顶部导航栏（返回、标题、清空）
- 桌台信息卡片
- 已选商品列表（可调整数量）
- 订单备注输入
- 底部结算栏（合计金额、提交订单按钮）

#### 接口依赖
- 无（本地数据）

#### 交互逻辑
1. 显示购物车中的所有商品
2. 可调整商品数量或删除
3. 点击"清空"：清空购物车
4. 点击"提交订单"：跳转支付页

---

### 4.5 支付页 (payment)

#### 功能描述
- 顶部导航栏
- 支付倒计时（14:59）
- 支付金额（大号显示）
- 订单信息（订单编号、桌号）
- 支付方式选择（微信支付、余额支付）
- 确认支付按钮

#### 接口依赖
- `POST /customer/orders` - 创建订单
- `POST /customer/payments/wechat` - 发起微信支付（当前MOCK）

#### 交互逻辑
1. 创建订单，获取订单编号
2. 选择支付方式
3. 点击"确认支付"：调用支付接口
4. 支付成功：跳转订单结果页

---

### 4.6 订单结果页 (order-result)

#### 功能描述
- 成功图标（大圆形）
- 支付成功提示
- 订单信息卡片（支付金额、桌台）
- 两个按钮（返回首页、查看订单）

#### 接口依赖
- 无（接收上一页传递的数据）

#### 交互逻辑
1. 显示支付结果
2. 点击"返回首页"：跳转首页
3. 点击"查看订单"：跳转订单详情页

---

### 4.7 订单详情页 (order-detail)

#### 功能描述
- 顶部导航栏
- 订单状态横幅（制作中、已完成等）
- 基本信息卡片（商家名称、桌号、商品列表、实付款）
- 订单信息（订单编号、下单时间、支付方式、备注）

#### 接口依赖
- `GET /customer/orders/{orderId}` - 获取订单详情

#### 交互逻辑
1. 根据订单ID获取订单详情
2. 显示订单状态和商品信息
3. 支持返回上一页

---

### 4.8 消息页 (message)

#### 功能描述
- 顶部导航栏
- 消息列表（卡片式）
- 消息类型标签（订单通知、活动推送）
- 消息时间
- 消息标题和内容预览

#### 接口依赖
- `GET /customer/messages` - 获取消息列表
- `PUT /customer/messages/{id}/read` - 标记消息已读

#### 交互逻辑
1. 加载消息列表
2. 显示未读消息红点
3. 点击消息：标记已读，显示详情

---

### 4.9 我的页 (mine)

#### 功能描述
- 顶部个人信息区（渐变背景）
  - 头像、昵称、会员等级
- 资产卡片（积分、优惠券）
- 菜单列表
  - 历史订单
  - 联系客服
  - 设置

#### 接口依赖
- `GET /customer/info` - 获取顾客信息
- `GET /customer/stats` - 获取积分、优惠券统计

#### 交互逻辑
1. 显示用户信息和资产
2. 点击"历史订单"：跳转订单历史页
3. 点击"联系客服"：拨打电话或跳转客服页
4. 点击"设置"：跳转设置页

---

### 4.10 订单历史页 (order-history)

#### 功能描述
- 顶部导航栏
- 订单列表（卡片式）
- 订单状态、时间、商品信息、金额
- 点击查看详情

#### 接口依赖
- `GET /customer/orders` - 获取订单列表

#### 交互逻辑
1. 加载订单列表
2. 支持下拉刷新
3. 点击订单：跳转订单详情页

---

### 4.11 登录页 (login)

#### 功能描述
- 品牌Logo和名称
- 手机号输入框
- 验证码输入框
- 获取验证码按钮（倒计时）
- 登录按钮
- 隐私协议勾选

#### 接口依赖
- `POST /customer/auth/send-code` - 发送验证码
- `POST /customer/auth/login` - 手机号登录

#### 交互逻辑
1. 输入手机号
2. 点击"获取验证码"：发送验证码，按钮倒计时60秒
3. 输入验证码
4. 点击"登录"：验证成功后跳转首页

---

## 五、API接口清单

### 5.1 认证相关
| 接口 | 方法 | 说明 | 状态 |
|------|------|------|------|
| `/customer/auth/send-code` | POST | 发送验证码 | ✅ 已实现 |
| `/customer/auth/login` | POST | 手机号登录 | ✅ 已实现 |

### 5.2 桌台相关
| 接口 | 方法 | 说明 | 状态 |
|------|------|------|------|
| `/customer/bar-tables` | GET | 获取可用桌台列表 | ✅ 已实现 |
| `/customer/bar-tables/current` | GET | 获取当前桌台信息 | ⚠️ 需实现 |
| `/customer/bar-tables/select` | POST | 选择桌台 | ⚠️ 需实现 |

### 5.3 商品相关
| 接口 | 方法 | 说明 | 状态 |
|------|------|------|------|
| `/customer/products/categories` | GET | 获取商品分类 | ✅ 已实现 |
| `/customer/products` | GET | 获取商品列表 | ✅ 已实现 |
| `/customer/products/recommendations` | GET | 获取推荐商品 | ⚠️ 需实现 |

### 5.4 订单相关
| 接口 | 方法 | 说明 | 状态 |
|------|------|------|------|
| `/customer/orders` | POST | 创建订单 | ✅ 已实现 |
| `/customer/orders` | GET | 获取订单列表 | ⚠️ 需实现 |
| `/customer/orders/{id}` | GET | 获取订单详情 | ✅ 已实现 |

### 5.5 支付相关
| 接口 | 方法 | 说明 | 状态 |
|------|------|------|------|
| `/customer/payments/wechat` | POST | 发起微信支付 | ✅ 已实现（MOCK） |

### 5.6 顾客相关
| 接口 | 方法 | 说明 | 状态 |
|------|------|------|------|
| `/customer/info` | GET | 获取顾客信息 | ✅ 已实现 |
| `/customer/stats` | GET | 获取积分、优惠券统计 | ⚠️ 需实现 |

### 5.7 消息相关
| 接口 | 方法 | 说明 | 状态 |
|------|------|------|------|
| `/customer/messages` | GET | 获取消息列表 | ✅ 已实现 |
| `/customer/messages/{id}/read` | PUT | 标记消息已读 | ⚠️ 需实现 |

---

## 六、数据流设计

### 6.1 点餐流程
```
1. 扫码/选择桌台 → 保存桌台信息到本地
2. 浏览菜单 → 加入购物车（本地存储）
3. 查看购物车 → 调整数量、添加备注
4. 提交订单 → POST /customer/orders
5. 确认支付 → POST /customer/payments/wechat
6. 支付成功 → 跳转订单结果页
7. 查看订单 → GET /customer/orders/{id}
```

### 6.2 本地存储
```javascript
// 桌台信息
wx.setStorageSync('currentTable', { area: '大厅', code: 'A08' })

// 购物车
wx.setStorageSync('cart', {
  1: { id: 1, name: '寻野特调·迷雾', price: 68, qty: 2 },
  2: { id: 2, name: '日落大道', price: 55, qty: 1 }
})

// 用户Token
wx.setStorageSync('customerToken', 'xxx')

// 用户信息
wx.setStorageSync('customerInfo', { id: 1, phone: '138****1234', ... })
```

---

## 七、技术实现要点

### 7.1 全局状态管理
使用 `app.js` 的 `globalData` 管理全局状态：
```javascript
App({
  globalData: {
    currentTable: null,
    cart: {},
    customerInfo: null,
    token: null
  }
})
```

### 7.2 请求封装
封装统一的请求方法，处理Token、错误等：
```javascript
// services/request.js
function request(url, method, data) {
  return new Promise((resolve, reject) => {
    wx.request({
      url: BASE_URL + url,
      method,
      data,
      header: {
        'Content-Type': 'application/json',
        'Authorization': wx.getStorageSync('customerToken')
      },
      success: (res) => {
        if (res.data.code === 200) {
          resolve(res.data.data)
        } else {
          wx.showToast({ title: res.data.message, icon: 'none' })
          reject(res.data)
        }
      },
      fail: reject
    })
  })
}
```

### 7.3 购物车管理
```javascript
// utils/cart.js
const CartManager = {
  // 添加商品
  add(product) { ... },

  // 更新数量
  updateQty(productId, qty) { ... },

  // 删除商品
  remove(productId) { ... },

  // 清空购物车
  clear() { ... },

  // 获取购物车
  getCart() { ... },

  // 计算总价
  getTotalPrice() { ... },

  // 计算总数量
  getTotalQty() { ... }
}
```

### 7.4 扫码功能
```javascript
wx.scanCode({
  success: (res) => {
    // 解析二维码内容，格式：xunye://table?code=A08
    const code = parseQRCode(res.result)
    selectTable(code)
  }
})
```

---

## 八、待实现功能

### 8.1 后端接口
- [ ] 获取当前桌台信息
- [ ] 选择桌台
- [ ] 获取推荐商品
- [ ] 获取订单列表
- [ ] 获取顾客统计信息
- [ ] 标记消息已读

### 8.2 前端功能
- [ ] 搜索商品
- [ ] 订单筛选（按状态）
- [ ] 优惠券选择
- [ ] 积分兑换
- [ ] 客服功能
- [ ] 设置页面

### 8.3 优化项
- [ ] 图片懒加载
- [ ] 下拉刷新、上拉加载
- [ ] 骨架屏
- [ ] 错误页面
- [ ] 网络异常处理

---

## 九、开发计划

### Phase 1: 核心功能（1-2天）
- [x] 项目初始化
- [ ] 全局配置和样式
- [ ] 首页
- [ ] 选桌页
- [ ] 菜单页
- [ ] 购物车页
- [ ] 支付页
- [ ] 订单结果页
- [ ] 订单详情页

### Phase 2: 会员功能（1天）
- [ ] 登录页
- [ ] 我的页
- [ ] 订单历史页
- [ ] 消息页

### Phase 3: 优化和测试（1天）
- [ ] 接口联调
- [ ] 异常处理
- [ ] 性能优化
- [ ] 真机测试

---

## 十、注意事项

1. **图片资源**：商品图片需要使用HTTPS链接或本地图片
2. **支付功能**：当前为MOCK，真实支付需要微信支付商户号
3. **扫码功能**：需要在微信开发者工具中测试
4. **Token管理**：登录后保存Token，请求时携带
5. **错误处理**：统一处理网络错误、业务错误
6. **用户体验**：加载状态、空状态、错误状态都要处理

---

## 附录：文件结构

```
xunye-miniapp/
├── app.js                  # 全局逻辑
├── app.json                # 全局配置
├── app.wxss                # 全局样式
├── pages/                  # 页面目录
│   ├── index/              # 首页
│   ├── table-select/       # 选桌页
│   ├── menu/               # 菜单页
│   ├── cart/               # 购物车页
│   ├── payment/            # 支付页
│   ├── order-result/       # 订单结果页
│   ├── order-detail/       # 订单详情页
│   ├── order-history/      # 订单历史页
│   ├── message/            # 消息页
│   ├── mine/               # 我的页
│   └── login/              # 登录页
├── services/               # API服务
│   ├── request.js          # 请求封装
│   ├── customer.js         # 顾客相关API
│   ├── product.js          # 商品相关API
│   ├── order.js            # 订单相关API
│   └── payment.js          # 支付相关API
├── utils/                  # 工具函数
│   ├── util.js             # 通用工具
│   ├── cart.js             # 购物车管理
│   └── validator.js        # 表单验证
└── static/                 # 静态资源
    └── images/             # 图片资源
```
